# wwwsqldesigner-provisioner-centos

reading throught the Ansible for DevOps book 

'''
$ vagrant box add geerlingguy/centos7
$ vagrant init geerlinugguy/centos7
    $ vagrant global-status
    $ vagrant global-status --prune
$ vagrant up
$ vagrant ssh
    $ vagrant ssh-config
'''